[weather_api_config]
weather_api_key = "${WEATHER_API_KEY}"
scheme = "https"
host = "api.weatherapi.com"
coords = "${COORDS}"
timeout = 10 # development, in live it should be >=60 sec

[influx_db_config]
host = "192.168.2.27"
port = 8086
database = "weatherapi_com"
token = "${INFLUXDB_TOKEN}"